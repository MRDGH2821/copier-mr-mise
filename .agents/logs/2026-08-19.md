# 2026-08-19T07:39:48+10:00

- Prompt: "the hooks in hk are fighting against each other. i want all yaml fixers to use double quotes."
- Author: MRDGH2821
- Model: Composer via Cursor
- Work completed:
  - Reproduced prettier vs yamlfix ping-pong on quoted YAML (`"true"` vs `'true'`).
  - Set yamlfix to preserve existing quotes and use `"` when it quotes (`preserve_quotes`, `quote_representation`).
  - Locked prettier YAML to `singleQuote: false` and yamllint `quoted-strings` to double quotes.
  - Ordered hk steps so prettier runs first, then yamlfix, then yamllint.
  - Converted remaining single-quoted strings in MegaLinter workflows.
