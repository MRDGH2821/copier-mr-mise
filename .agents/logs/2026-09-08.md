# 2026-09-08

## 2026-09-08T16:08:19+10:00 — Fix apm.yml for APM 0.30 skills subset regression

**Prompt / request:** Switch `apm.yml` to use `path:` instead of `skills:` so `apm install` works on APM 0.30; earlier diagnosis found `skills:` subset installs fail on 0.29+/0.30.

**Author:** MRDGH2821

**What was done:**

- Changed `vercel-labs/skills` dep from `skills: [find-skills]` to `path: skills/find-skills` (also restored correct repo URL; it had been briefly set to `agent-skills`).
- Changed `github/awesome-copilot` dep to `path: skills/git-commit`.
- Kept megalinter pin at `v10.1.0`.
- Note: `path: skills` + `skills: [name]` still fails on APM 0.30 — full path to the skill dir is required.
- Ran `apm install` successfully; regenerated `apm.lock.yaml`; skills deployed under `.agents/skills/` and `.claude/skills/`.

**Model:** Composer via Cursor
