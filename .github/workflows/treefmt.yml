---
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: jdx/mise-action@v3
      - run: mise run fmt
name: Treefmt (via mise)
on: # yamllint disable-line rule:truthy
  pull_request:
  push:
